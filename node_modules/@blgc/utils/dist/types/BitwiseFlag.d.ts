export declare class BitwiseFlag<T extends number> {
    private value;
    constructor(...initialValues: T[]);
    add(flag: T): this;
    remove(flag: T): this;
    has(flag: T): boolean;
    reset(): this;
}
export declare function bitwiseFlag<T extends number>(...values: T[]): BitwiseFlag<T>;
